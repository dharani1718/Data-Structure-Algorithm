def solve(arr, n):
    temp = [0] * n 
    for i in range(1, n):
        temp[i - 1] = arr[i]
    temp[n - 1] = arr[0]  
    for num in temp:
        print(num, end=" ") 
    print()

if __name__ == "__main__":
    n = 5 
    arr = [1, 2, 3, 4, 5] 
    solve(arr, n)